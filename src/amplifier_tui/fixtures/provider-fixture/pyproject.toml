[project]
name = "amplifier-module-provider-fixture"
version = "0.1.0"
dependencies = ["amplifier-core>=1.6.1"]
[project.entry-points."amplifier.modules"]
provider-fixture = "amplifier_module_provider_fixture:mount"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

