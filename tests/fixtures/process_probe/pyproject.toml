[project]
name = "amplifier-module-tool-process-probe"
version = "0.0.0"
description = "Offline subprocess boundary test fixture"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["amplifier_module_tool_process_probe"]
