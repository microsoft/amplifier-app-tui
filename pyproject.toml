[project]
name = "amplifier-app-tui"
version = "0.3.0rc3"
description = "A modular terminal host for Amplifier"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "amplifier-core==1.6.1",
  "amplifier-foundation @ git+https://github.com/microsoft/amplifier-foundation@e210edabd947af82d5121a240d6934283ac540b9",
  "textual==8.2.8",
  "pillow==12.1.1",
]

[project.scripts]
amplifier-tui = "amplifier_tui.launcher:main"
amplifier-tui-host = "amplifier_tui.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build]
exclude = ["**/*.zip", "**/node_modules/**", "**/target/**", ".state/**", ".evidence/**", ".venv/**"]

[tool.hatch.build.targets.wheel]
packages = ["src/amplifier_tui"]

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"

[tool.hatch.build.targets.sdist]
include = ["src", "frontends/ratatui/src", "frontends/ratatui/Cargo.toml", "frontends/ratatui/Cargo.lock", "hatch_build.py", "pyproject.toml", "README.md"]

[dependency-groups]
dev = [
    "pillow==12.1.1",
    "pyte==0.8.2",
    "pytest>=8",
    "pytest-asyncio>=1",
    "ruff>=0.12",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
